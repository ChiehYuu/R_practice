# function build
#my_function(my_inpus, my_args, ...)

#abs() 絕對值
-5:-1
abs(-5:-1)

#sqrt() 根號
1:5
sqrt(1:5)

#ceiling() 無條件進位
ceiling(9.527)
nums <- sqrt(c(7, 17, 27, 37, 47))
nums
ceiling(nums)

#floor() 無條件去位
floor(9.527)
nums <- sqrt(c(7, 17, 27, 37, 47))
nums
floor(nums)

#